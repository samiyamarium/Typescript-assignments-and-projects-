//Exercise 28
var age = 0;
console.log(age);
if (age < 2)
    console.log("age is <2:".concat("The person is a baby"));
else
    (typeof console == "undefined");
{
    //nothing to do
}
if (age >= 2 || age < 4)
    console.log("age is less than 4 but greater than 2:".concat("The person is a toddler"));
else
    (typeof console == "undefined");
{
    //nothing to do
}
if (age >= 4 || age < 13)
    console.log("age is less than 13 but greater tha 4:".concat("The person is a kid"));
else
    (typeof console == "undefined");
{
    //nothing to do
}
if (age >= 13 || age < 20)
    console.log("age is less than 20 but greater tha 13:".concat("The person is a teenager"));
else
    (typeof console == "undefined");
{
    //nothing to do
}
if (age >= 20 || age < 65)
    console.log("age is less than 65 but greater tha 20:".concat("The person is an adult"));
else
    (typeof console == "undefined");
{
    //nothing to do
}
if (age <= 2 || age > 65)
    console.log("age is greater than 65:".concat("The person is an elder"));
else
    (typeof console == "undefined");
{
    //nothing to do
}
