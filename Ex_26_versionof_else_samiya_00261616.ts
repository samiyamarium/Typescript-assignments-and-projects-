var alien_color=["green","yellow","red"]
//Version that runs only else statement
if (typeof console==="undefined");{
    //nothing to do
    }
    else(alien_color[0]==alien_color[0]);{
    console.log("\n Running only else statement, if statement null")}
    
    //results of version 2
    //returning error=The body of an 'if'statement cannot be empty. (when {} are not used for body)
    // returing declaration expecting error for else statement; when {} are applied to body of if statement
    
    
    
        
    
    